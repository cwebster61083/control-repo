input {
    stdin { }
}

filter{
  grok { 
    match => { "message" => 
      "%{COMBINEDAPACHELOG}"
      }
    }
    date {
       match => [ "timestamp", "dd/MMM/yyyy:HH:mm:ss XX"  ]
       target => "timestamp"
    }
}

output {
  stdout { codec => rubydebug }
}